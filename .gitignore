# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
.logs/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Environment variables with secrets
.env.local

# Personal resume data (keep template, gitignore personal data)
data/resume-data.json

# Session/local files
.claude/

# ============================================
# Rust + WASM Build Artifacts
# ============================================
# Generated when cargo init wasn't used, adding manually
# See: https://github.com/github/gitignore/blob/main/Rust.gitignore

# Rust build artifacts (6.6GB+ during development)
target/
**/target/

# Rust backup files
**/*.rs.bk

# Debug symbols (Windows)
*.pdb

# WASM-specific
pkg/
**/pkg/
wasm-pack.log

# WASM build artifacts are TRACKED for fast Vercel deploys
# Pre-commit hook ensures they're always up-to-date
# !public/wasm/*.wasm
# !public/wasm/*.js
# .wasm-build-hash

# Rust/WASM test outputs
target/test-pdfs/
test-outputs/

# Generated PDFs (never commit - contains PII)
*.pdf
pdf-logs/

# TypeScript build artifacts
tsconfig.tsbuildinfo

# Bun lockfile (similar to package-lock.json)
bun.lock

# macOS artifacts
.DS_Store
**/.DS_Store

# Temp/backup files
*.bak
*.backup
*.tmp
*~

# Note: Cargo.lock is intentionally tracked for workspace reproducibility
# Only libraries should ignore Cargo.lock, applications should commit it
